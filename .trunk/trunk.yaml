# This file controls the behavior of Trunk: https://docs.trunk.io/cli
# To learn more about the format of this file, see https://docs.trunk.io/reference/trunk-yaml
version: 0.1
cli:
  version: 1.25.0
# Trunk provides extensibility via plugins. (https://docs.trunk.io/plugins)
plugins:
  sources:
    - id: trunk
      ref: v1.11.0
      uri: https://github.com/trunk-io/plugins
# Many linters and tools depend on runtimes - configure them here. (https://docs.trunk.io/runtimes)
runtimes:
  enabled:
    - go@1.21.0
    - java@13.0.11
    - node@22.16.0
    - python@3.14.4
# This is the section where you manage your linters. (https://docs.trunk.io/check/configuration)
lint:
  disabled:
    - ktlint
  enabled:
    - cspell@10.0.1
    - checkov@3.3.10
    - git-diff-check
    - markdownlint@0.49.1
    - prettier@3.9.6
    # trivy was dropped: both pins 404 on their macOS-ARM64 release asset, and with no
    # lockfile, image or IaC in this repo it had nothing to read anyway.
    - osv-scanner@2.5.0
    - shellcheck@0.11.0
    - shfmt@3.13.1
    - taplo@0.10.0
    - trufflehog@3.96.0
    - yamllint@1.38.0
  ignore:
    # Gradle-generated; reformatting or "fixing" these breaks the wrapper contract.
    - linters: [ALL]
      paths:
        - gradlew
        - gradlew.bat
        - gradle/wrapper/**
    # Vendored Google reference material, not this repository's prose.
    - linters: [ALL]
      paths:
        - .agents/**
actions:
  enabled:
    - trunk-announce
    - trunk-check-pre-push
    - trunk-fmt-pre-commit
    - trunk-upgrade-available
